"""CarMaker semantic segmentation package.

이 파일은 `src/segmentation` 폴더를 Python package로 인식하게 만든다.
그래야 아래처럼 package import를 안정적으로 사용할 수 있다.

    from segmentation.models import build_model
    from segmentation.dataset import SegmentationDataset

파일 안에 실행 로직은 없고, import 구조를 명확하게 하기 위한 marker 역할이다.
"""
